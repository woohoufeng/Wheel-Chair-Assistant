#:include farmersmapview.kv

BoxLayout:
    orientation: 'vertical'
    MDToolbar:
        title: "Wheel Chair Assistant"
        right_action_items: [['magnify', lambda x: app.search_menu.open()]]
        md_bg_color: app.theme_cls.primary_color
    FarmersMapView:
        id: mapview
